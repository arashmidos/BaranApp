<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="org.baran"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="AA_DB_NAME"
            android:value="baranapp.db"/>
        <meta-data
            android:name="AA_DB_VERSION"
            android:value="6"/>
        <meta-data
            android:name="AA_MODELS"
            android:value="org.baran.model.Category,org.baran.model.Jobs,org.baran.model.Favourite,org.baran.model.Notification"/>

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".Notifications.Notification_message"
            android:label="@string/app_name">

        </activity>
        <activity
            android:name=".activities.TestFav"
            android:label="@string/app_name">
            <!--<intent-filter>-->
                <!--<action android:name="android.intent.action.MAIN"/>-->

                <!--<category android:name="android.intent.category.LAUNCHER"/>-->
            <!--</intent-filter>-->
        </activity>
    </application>

</manifest>
